{% include(option) with 
{ name: 'className', 
  default: 'ol-layer' } %}

{% include(option) with 
{ name: 'maxResolution', 
  type: 'number' } %}

{% include(option) with 
{ name: 'minResolution', 
  type: 'number' } %}

{% include(option) with 
{ name: 'maxZoom', 
  type: 'number' } %}

{% include(option) with 
{ name: 'minZoom', 
  type: 'number' } %}

{% include(option) with 
{ name: 'opacity', 
  type: 'number', 
  default: 1 } %}

{% include(option) with 
{ name: 'preload', 
  type: 'number', 
  default: 0 } %}

{% include(option) with 
{ name: 'useInterimTilesOnError', 
  type: 'boolean', 
  default: true } %}

{% include(option) with 
{ name: 'visible', 
  type: 'boolean', 
  default: true } %}

{% include(option) with 
{ name: 'zIndex', 
  type: 'number' } %}

{% include(option) with 
{ name: 'source' } %}
