{% include(option) with 
{ name: 'bar', 
  type: 'boolean', 
  default: false } %}

{% include(option) with 
{ name: 'dpi', 
  type: 'number' } %}

{% include(option) with 
{ name: 'maxWidth', 
  type: 'number' } %}

{% include(option) with 
{ name: 'minWidth', 
  type: 'number', 
  default: 64 } %}

{% include(option) with 
{ name: 'steps', 
  type: 'number', 
  default: 4 } %}

{% include(option) with 
{ name: 'text', 
  type: 'boolean', 
  default: false } %}

{% include(option) with 
{ name: 'target' } %}

{% include(option) with 
{ name: 'className' } %}
