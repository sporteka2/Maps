{% include(option) with 
{ name: 'collapsed', 
  'type': 'boolean',
  default: '1' } %}

{% include(option) with 
{ name: 'collapsible', 
  'type': 'boolean', 
  default: '1' } %}

{% include(option) with 
{ name: 'rotateWithView', 
  'type': 'boolean' } %}

{% include(option) with 
{ name: 'target' } %}

{% include(option) with 
{ name: 'className', 
  default: 'ol-overviewmap' } %}
