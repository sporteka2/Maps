{% include(option) with 
{ name: 'projection', 
  type: 'ProjectionLike',
  default: '' } %}

{% include(option) with 
{ name: 'placeholder' } %}

{% include(option) with 
{ name: 'wrapX', 
  type: 'boolean', 
  default: true } %}

{% include(option) with 
{ name: 'advanced',
  values: [
    { name: 'target' },
    {name: 'className', default: 'ol-mouse-position'} ] } %}
