{% include(option) with 
{ name: 'projection', 
  type: 'ProjectionLike' } %}

{% include(option) with 
{ name: 'placeholder' } %}

{% include(option) with 
{ name: 'wrapX', 
  type: 'boolean', 
  default: true } %}

{% include(option) with 
{ name: 'target' } %}

{% include(option) with 
{ name: 'className',
  default: 'ol-mouse-position' } %}
