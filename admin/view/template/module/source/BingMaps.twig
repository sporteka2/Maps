{% include(option) with 
{ name: 'cacheSize', 
  type: 'number' } %}

{% include(option) with 
{ name: 'interpolate', 
  type: 'boolean', 
  default: 1 } %}

{% include(option) with 
{ name: 'reprojectionErrorThreshold', 
  type: 'number', 
  default: 0.5 } %}

{% include(option) with 
{ name: 'wrapX', 
  type: 'boolean', 
  default: 1 } %}

{% include(option) with 
{ name: 'transition', 
  type: 'number' } %}

{% include(option) with 
{ name: 'zDirection', 
  type: 'number', 
  default: 0 } %}

{% include(option) with 
{ name: 'culture',
  default: 'en-us' } %}

{% include(option) with 
{ name: 'hidpi', 
  type: 'boolean' } %}

{% include(option) with 
{ name: 'imagerySet' } %}

{% include(option) with 
{ name: 'key' } %}

{% include(option) with 
{ name: 'maxZoom', 
  type: 'number', 
  default: 21 } %}

{% include(option) with 
{ name: 'placeholderTiles', 
  type: 'boolean' } %}
