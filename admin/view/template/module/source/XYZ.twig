{% include(option) with 
{ name: 'attributions', 
  type: 'AttributionLike' } %}

{% include(option) with 
{ name: 'attributionsCollapsible', 
  type: 'boolean', 
  default: true } %}

{% include(option) with 
{ name: 'cacheSize', 
  type: 'number' } %}

{% include(option) with 
{ name: 'crossOrigin' } %}

{% include(option) with 
{ name: 'interpolate', 
  type: 'boolean', 
  default: 1 } %}

{% include(option) with 
{ name: 'opaque', 
  type: 'boolean' } %}

{% include(option) with 
{ name: 'projection', 
  type: 'ProjectionLike', 
  default: 'EPSG:3857' } %}

{% include(option) with 
{ name: 'reprojectionErrorThreshold', 
  type: 'number', 
  default: 0.5 } %}

{% include(option) with 
{ name: 'maxZoom', 
  type: 'number', 
  default: 42 } %}

{% include(option) with 
{ name: 'minZoom', 
  type: 'number', 
  default: 0 } %}

{% include(option) with 
{ name: 'maxResolution', 
  type: 'number' } %}

{% include(option) with 
{ name: 'tilePixelRatio', 
  type: 'number', 
  default: 1 } %}

{% include(option) with 
{ name: 'wrapX', 
  type: 'boolean', 
  default: 1 } %}

{% include(option) with 
{ name: 'transition', 
  type: 'number', 
  default: 250 } %}

{% include(option) with 
{ name: 'zDirection', 
  type: 'number', 
  default: 0 } %}

{% include(option) with 
{ name: 'url' } %}

{% include(option) with 
{ name: 'gutter', 
  type: 'number', 
  default: 0 } %}
