<?php

// Labels


// <editor-fold desc="Attributions" defaultstate="collapsed">
 
// Text label to use for the button tip.
$_['control.attribution.tipLabel'] = 'Attributions';

// Text label to use for the collapsed attributions button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.attribution.label'] = 'i';

// Text label to use for the expanded attributions button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.attribution.collapseLabel'] = '›';

// </editor-fold>

// <editor-fold desc="FullScreen" defaultstate="collapsed">

// Text label to use for the button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.fullScreen.label'] = '\u2922';

// Text label to use for the button when full-screen is active. Instead of text, also an element (e.g. a span element) can be used.
$_['control.fullScreen.labelActive'] = '\u00d7';

// Text label to use for the button tip.
$_['control.fullScreen.tipLabel'] = 'Toggle full-screen';

// </editor-fold>

// <editor-fold desc="OverviewMap" defaultstate="collapsed">

// Text label to use for the expanded overviewmap button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.overviewMap.collapseLabel'] = '‹';

// Text label to use for the collapsed overviewmap button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.overviewMap.label'] = '›';

// Text label to use for the button tip.
$_['control.overviewMap.tipLabel'] = 'Overview map';

// </editor-fold>

// <editor-fold desc="Rotate" defaultstate="collapsed">

// Text label to use for the rotate button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.rotate.label'] = '⇧';

// Text label to use for the rotate tip.
$_['control.rotate.tipLabel'] = 'Reset rotation';

// </editor-fold>

// <editor-fold desc="ZoomToExtent" defaultstate="collapsed">

// Text label to use for the button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.zoomToExtent.label'] = 'E';

// Text label to use for the button tip.
$_['control.zoomToExtent.tipLabel'] = 'Fit to extent';

// </editor-fold>

// <editor-fold desc="Zoom" defaultstate="collapsed">

// Text label to use for the zoom-in button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.zoom.zoomInLabel'] = '+';

// Text label to use for the zoom-out button. Instead of text, also an element (e.g. a span element) can be used.
$_['control.zoom.zoomOutLabel'] = '–';

// Text label to use for the button tip.
$_['control.zoom.zoomInTipLabel'] = 'Zoom in';

// Text label to use for the button tip.
$_['control.zoom.zoomOutTipLabel'] = 'Zoom out';

// </editor-fold>
